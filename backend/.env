# LOCAL config. Postgres được map ra cổng 5432 của máy bởi docker-compose.
# Dùng file này khi chạy `go run .` bên ngoài docker.
export DATABASE_URL="postgres://postgres:pass@localhost:5433/todo?sslmode=disable"
export PORT="8080"
export ALLOWED_ORIGIN="*"
